# ---
# - name: playing with facts
#   hosts: localhost
#   gather_facts: true

  # tasks:
  # - debug: 
  #     var: ansible_facts 

  # - debug: 
    #   msg: Host "{{ ansible_ }}"

  # - debug: 
    #   msg: Host "{{ ansible_ }}" Linux distribution is 

  # - debug: 
  #     msg: less than 2000mb
  #   when: ansible_ <= 2000

  # - debug: 
  #     msg: more than 2000mb
  #   when: ansible_ >= 2000

  